<!DOCTYPE html>
<html>
  <head>
    <title>Laba12</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <style type="text/css">
      table {
        width: 50vw;
      }
      td, th, table {
        border: 1px solid darkcyan;
        border-collapse: collapse;
        text-align: center;
      }
      td, th {
        padding: 10px;
      }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to 'Palindromes', '/palindromes/input', class: 'nav-link active' %>
            </li>
            <% if session[:user_id].nil? %>
              <li class="nav-item">
                <%= link_to 'Sign in', '/sessions/new', class: 'nav-link active' %>
              </li>
              <li class="nav-item">
                <%= link_to 'Sign up', '/users/new', class: 'nav-link active' %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to 'Sign out', sessions_destroy_path, data: {turbo_method: :delete}, class: 'nav-link active' %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <br/>
    <div class="container">
      <% flash.each do |type, msg| %>
        <% if type == 'success' %>
          <div class="alert alert-success">
            <%= msg %>
          </div>
        <% elsif type == 'warning' %>
          <div class="alert alert-danger">
            <%= msg %>
          </div>
        <% end %>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
